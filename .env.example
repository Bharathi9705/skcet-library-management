# Server
PORT=5000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=library_db
DB_PORT=3306

# JWT
JWT_SECRET=skcet_library_super_secret_key_change_in_production
JWT_EXPIRES_IN=7d

# Fine per day (in INR)
FINE_PER_DAY=2.00

# Loan period (days)
DEFAULT_LOAN_DAYS=14
